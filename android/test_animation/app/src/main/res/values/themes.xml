<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Test_animation" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowEnterAnimation">@transition/default_fade</item>
        <item name="android:windowExitAnimation">@transition/default_fade</item>

        <item name="android:windowSharedElementEnterTransition">@transition/change_image_transition</item>
        <item name="android:windowSharedElementExitTransition">@transition/change_image_transition</item>
    </style>

    <style name="Theme.Test_animation" parent="Base.Theme.Test_animation" />
</resources>